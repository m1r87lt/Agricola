/*
 * state_machine.h
 *
 *  Created on: 03 apr 2018
 *      Author: MLaRosa
 */

#ifndef STATE_MACHINE_H_
#define STATE_MACHINE_H_

#include "management.h"
/*
bool new_stage();
std::string status();
std::string instant();
unsigned stage();
unsigned round();
bool harvest();
unsigned phase();
unsigned step();
unsigned stage();
std::list<std::string>& triggers();

void phase_start__1(base::Log track = base::Log());
void phase_end__2(base::Log track = base::Log());
void phase1_draw_a_new_round_card_1_3(base::Log track = base::Log());
void phase1_start_the_round_1_4(base::Log track = base::Log());
void phase2_replenish_2_3(base::Log track = base::Log());
void phase3_work_3_3(base::Log track = base::Log());
void phase3_work_3_(base::Log track = base::Log());
void phase4_return_home_4_3(base::Log track = base::Log());
void phase4_return_home_4_(base::Log track = base::Log());
void harvest_phase1_field_1_3(base::Log track = base::Log());
void harvest_phase1_field_1_(base::Log track = base::Log());
void harvest_phase2_feeding_the_family_2_3(base::Log track = base::Log());
void harvest_phase2_feeding_the_family_2_(base::Log track = base::Log());
void harvest_phase3_breeding_3_3(base::Log track = base::Log());
void harvest_phase3_breeding_3_(base::Log track = base::Log());
void round_start(base::Log track = base::Log());
void round_end(base::Log track = base::Log());
void harvest_start(base::Log track = base::Log());
void stage_start(base::Log track = base::Log());
void stage_end(base::Log track = base::Log());
*/
#endif /* STATE_MACHINE_H_ */
